# Tictactoe-AlphaZero
An implementation of AlphaZero's self-play algorithm from scratch to create a super-human tic-tac-toe AI. 
The model can be reused for other (general) purposes. Only the rules of the domain need to be explicitly stated.
For example, the model can be used for chess, and chess rules and logic must be defined. 
